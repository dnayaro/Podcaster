import { FiltroPodcastPipe } from './filtro-podcast.pipe';

describe('FiltroPodcastPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPodcastPipe();
    expect(pipe).toBeTruthy();
  });
});
